# MCP server — only needs to know where Readeck lives.
# Tokens are NOT stored on the server: each MCP client passes its own
# Readeck token via the X-API-Key header when connecting.

READECK_BASE_URL=https://read.example.com

# Optional. Default is :8080.
# MCP_HTTP_ADDR=:8080
